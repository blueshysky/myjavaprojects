import java.util.*;

public class jason
{

	public static void main(String[] args)
	{
      
	  /*
	  Scanner scan = new Scanner(System.in);
      
      System.out.println("Enter a word for this exercise.");
      String word = scan.next();
      
      char hold = word.charAt(0);
      char extra;
      
      word = word.substring(1);
      
      word = word + hold;
      
      for(int i = 0; i < word.length(); i++)
      {
    	  String copy = word;
    	  extra = word.charAt(0);
    	  copy.charAt(i) = word.charAt((word.length-1)-i);
      }
      
      System.out.println(word);
      
      */
		
//incomplete code
//Needs to reverse the string
      
   }
}